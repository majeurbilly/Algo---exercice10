#include "stdafx.h"
#include "CppUnitTest.h"
#include "Stack.h"
using namespace Microsoft::VisualStudio::CppUnitTestFramework;

namespace TestsCode
{
  TEST_CLASS(TestsStack){
    public :

        TEST_METHOD(Constructor){
            /*
              // TODO: Ici, votre code de test
              Stack stack;

              Assert::IsTrue(stack.isEmpty());
              Assert::AreEqual((unsigned int)0, stack.size());

            */
        }
    // Compl�tez les tests ici
    // Exemples de tests � produire:
    // Tester un push sur pile vide (v�rifiez la taille, si la pile est vide, le contenu du top)
    //  ...

    // Plus difficile car syntaxe diff�rente avec les exceptions
    // Tester un pop d'une pile vide (exception)
    // ...

    // Vous devez impl�menter tous les tests visant � mettre � l'�preuve votre structure.
    // Vous pouvez utiliser la technique de votre choix pour la génération des tests.
  };
}